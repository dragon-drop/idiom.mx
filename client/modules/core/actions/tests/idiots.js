const {describe, it} = global;
import {expect} from 'chai';
import {spy, stub} from 'sinon';
import actions from '../idiots';

describe('core.actions.idiots', () => {
  it('should do something');
});
